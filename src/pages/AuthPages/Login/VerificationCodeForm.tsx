import React from 'react';

interface VerificationCodeFormProps {
}

const VerificationCodeForm: React.FC<VerificationCodeFormProps> = (props) => {
  return (
    <p>VerificationCodeForm</p>
  );
};

export default VerificationCodeForm;
