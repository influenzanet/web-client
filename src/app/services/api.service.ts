import { Injectable } from '@angular/core';
import { HttpClient, HttpParams } from '@angular/common/http';

@Injectable()
export class ApiService {
    // TODO: this service is an abstraction layer to the http api of the backend services
}
