import { QuestionGroup, Question } from 'survey-engine/lib/data_types';

const q1: Question = {

}


export const testSurvey1: QuestionGroup = {
  items: [
    q1,
  ]
};

// JSON.stringify(testSurvey1, undefined, 2);
